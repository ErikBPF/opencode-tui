/target
**/*.rs.bk
.env
*.secrets.json

# devenv writes local state here; devenv.lock is committed on purpose.
.devenv/

# Pinned upstream source used only as an interface reference. Never built,
# never a dependency; recreated with `just reference`.
/reference/
